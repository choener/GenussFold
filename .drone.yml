kind: pipeline
name: DPutils test

steps:
  - name: stepOne
    image: haskell
    commands:
      - ls
      - which cabal
      - cabal update
      - cabal new-test
      - ls -alh /root/.cabal

#pipeline:
#  build:
#    image: plugins/git
#  commands:
#    - which cabal
